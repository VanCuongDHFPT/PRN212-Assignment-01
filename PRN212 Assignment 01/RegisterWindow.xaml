<Window x:Class="PRN212_Assignment_01.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRN212_Assignment_01"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Border Background="White"
             CornerRadius="10"
             Padding="20"
             VerticalAlignment="Center">
            <StackPanel>
                <!-- Title -->
                <TextBlock Text="Create Account"
                        FontSize="24"
                        FontWeight="Bold"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,20"/>

                <!-- Username -->
                <TextBlock Text="Email" FontWeight="SemiBold" Margin="5,0,0,5"/>
                <TextBox Name="EmailTextBox" Height="40" Padding="10" Background="#FAFAFA" BorderBrush="#CCC" BorderThickness="1"/>

                <!-- Password -->
                <TextBlock Text="Password" FontWeight="SemiBold" Margin="5,15,0,5"/>
                <PasswordBox Name="PasswordBox" Height="40" Padding="10" Background="#FAFAFA" BorderBrush="#CCC" BorderThickness="1"/>

                <!-- Confirm Password -->
                <TextBlock Text="Confirm Password" FontWeight="SemiBold" Margin="5,15,0,5"/>
                <PasswordBox Name="ConfirmPasswordBox" Height="40" Padding="10" Background="#FAFAFA" BorderBrush="#CCC" BorderThickness="1"/>

                <!-- Register Button -->
                <Button Content="Register"
                     Height="40"
                     FontSize="16"
                     FontWeight="Bold"
                     Background="#0078D7"
                     Foreground="White"
                     Cursor="Hand"
                     BorderThickness="0"
                     Margin="0,20,0,10" Click="Button_Click" 
                     >
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#0078D7"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#005A9E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Switch to login -->
                <TextBlock HorizontalAlignment="Center" FontSize="13">
                <Run Text="Already have an account? "/>
                <Hyperlink x:Name="lnkLogin" Click="LnkLogin">
                    Login here
                </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>
